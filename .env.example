# Variables de entorno para desarrollo local (copiar a .env)
# En Docker, las build args se definen en docker-compose.yml

# URL base del API (Vite la inyecta en build time)
# En producción con Nginx, "/api" es suficiente: el proxy reenvía al backend.
VITE_API_BASE_URL=/api

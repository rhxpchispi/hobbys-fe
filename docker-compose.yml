# =============================================================================
# docker-compose.yml — Ecosistema Hobbys (Qdrant + Backend FastAPI + Frontend)
# =============================================================================
# Uso:
#   docker compose up --build -d
#   Abrir http://localhost en el navegador
#
# Si ya tenés un docker-compose con backend y qdrant, copiá únicamente la
# sección `frontend` y la red `hobbys-network` (o uní las redes existentes).
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # Qdrant — Base de datos vectorial
  # ---------------------------------------------------------------------------
  qdrant:
    image: qdrant/qdrant:v1.13.2
    container_name: hobbys-qdrant
    restart: unless-stopped
    ports:
      # Exponer solo si necesitás acceder desde fuera de Docker (debug/admin)
      - "6333:6333"
    volumes:
      # Persistencia de colecciones e índices vectoriales
      - qdrant_data:/qdrant/storage
    networks:
      - hobbys-network
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333'"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ---------------------------------------------------------------------------
  # Backend — FastAPI (Python)
  # ---------------------------------------------------------------------------
  backend:
    # Ajustá la ruta de build o reemplazá por `image:` si publicás en un registry
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: hobbys-backend
    restart: unless-stopped
    ports:
      # API accesible en http://localhost:8000 (útil para Swagger/docs y debug)
      - "8000:8000"
    environment:
      # URL interna de Qdrant (nombre del servicio Docker, no localhost)
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      # Orígenes permitidos por CORS (frontend público)
      CORS_ORIGINS: "http://localhost,http://localhost:80,http://127.0.0.1"
    depends_on:
      qdrant:
        condition: service_healthy
    networks:
      - hobbys-network
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/docs')\" || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ---------------------------------------------------------------------------
  # Frontend — SPA React (Vite) + Nginx  [NUEVO SERVICIO]
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Vite inyecta esta variable en build time (opcional si usás rutas relativas /api)
        VITE_API_BASE_URL: /api
    container_name: hobbys-frontend
    restart: unless-stopped
    ports:
      # SPA accesible públicamente en http://localhost
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      # Misma red virtual → resolución DNS interna: http://backend:8000
      - hobbys-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

# =============================================================================
# Redes
# =============================================================================
networks:
  hobbys-network:
    driver: bridge
    name: hobbys-network

# =============================================================================
# Volúmenes
# =============================================================================
volumes:
  qdrant_data:
    name: hobbys-qdrant-data
